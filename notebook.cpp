#include "Notebook.hpp"
#include <iostream>
/*
    Notebook.cpp.
    Author: Omer Rabin.
*/
using namespace std;

namespace ariel {
    void Notebook::write(uint page, uint row, uint col, Direction direction, string message) 
    {
        return;
    }

    string Notebook::read(uint page, uint row, uint col, Direction direction, uint length){
        return "";
    }
        
    void Notebook::show(uint page) {
        return;
    }
    void Notebook::erase(uint page, uint row, uint col, Direction direction, uint length){
        return;
    }
}
